export default function ContactPage() {
  return (
    <div className="page-wrap">
      <h1 className="hero-title" style={{ fontSize: "clamp(2rem, 5vw, 3.2rem)", marginBottom: "0.4em" }}>
        Contact
      </h1>
      <div className="hero-copy" style={{ maxWidth: "760px" }}>
        <p className="hero-sub" style={{ marginTop: 0 }}>
          For exhibitions, commissions, and collaborations, use Instagram DM for now. This page is now part of the new
          Next.js structure and ready for form integration in the next step.
        </p>
      </div>
    </div>
  );
}
